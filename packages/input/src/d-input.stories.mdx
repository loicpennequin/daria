import { ref } from 'vue';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DInput } from '.';

<Meta title="Components/Input" />

# D-input

TODO

export const Template = args => ({
  components: {
    DInput
  },
  setup() {
    const value = ref('');
    return { value };
  },
  template: `
    <DInput v-model="value" placeholder="Enter your value" right-icon="chevronRight" />
    <pre>value: {{value}}</pre>`
});

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
