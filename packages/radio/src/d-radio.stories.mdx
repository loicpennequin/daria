import { ref } from 'vue';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DRadio, DRadioGroup } from '.';

<Meta title="Components/Radio" />

# D-radio

TODO

export const Template = args => ({
  components: {
    DRadio
  },
  setup() {
    const value = ref('apple');
    return { value };
  },
  template: `
    <DRadio v-model="value" value="apple" :is-inline="false">Apple</DRadio>
    <DRadio v-model="value" value="orange" :is-inline="false">Orange</DRadio>
    <DRadio v-model="value" value="banana" :is-inline="false">Banana</DRadio>
    <DRadio v-model="value" value="strawberry" :is-inline="false" disabled>Strawberry</DRadio>
    <pre>value: {{value}}</pre>
    `
});

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

## Using D-radio-group

export const RadioGroupTemplate = args => ({
  components: {
    DRadioGroup
  },
  setup() {
    const value = ref('apple');
    const values = [
      { label: 'Apple', value: 'apple' },
      { label: 'Orange', value: 'orange' },
      { label: 'Banana', value: 'banana' },
      { label: 'Strawberry', value: 'strawberry', disabled: true }
    ];
    return { values, value };
  },
  template: `
    <DRadioGroup v-model="value" :values="values"  />
    <pre>value: {{value}}</pre>
    `
});

<Canvas>
  <Story name="RadioGroup">{RadioGroupTemplate.bind({})}</Story>
</Canvas>
