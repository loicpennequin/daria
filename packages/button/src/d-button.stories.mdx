import { ref } from 'vue';
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import DButton from './d-button.vue';

<Meta title="Components/Button" component={DButton} />

# D-button

TODO

export const Template = args => ({
  components: { DButton },
  template: `
  <DButton>Click</DButton>`
});

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

export const FullWidthTemplate = args => ({
  components: { DButton },
  template: `<DButton is-fullwidth>Click</DButton>`
});

<Canvas>
  <Story name="Full Width">{FullWidthTemplate.bind({})}</Story>
</Canvas>

export const WithIconTemplate = args => ({
  components: { DButton },
  template: `
  <DButton left-icon="close">Click</DButton>`
});

<Canvas>
  <Story name="With Icon">{WithIconTemplate.bind({})}</Story>
</Canvas>
